// import { composeStories } from '@storybook/react';
// import { render, screen } from '@test-utils';
// import * as stories from './TaskDashboard.stories';

describe('<TaskDashboard />', () => {
  it('should mount', () => {
    // TODO: fix unit test by providing context provider in story
    // const { Primary } = composeStories(stories);
    // render(<Primary />);
    // expect(screen.getByTestId('TaskDashboard')).toBeInTheDocument();
  });
});
